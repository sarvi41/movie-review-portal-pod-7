# Movie Review Portal Backend

Spring Boot 3 scaffold for the Movie Review Portal.

## Build and Run

- `mvn clean package`
- `mvn spring-boot:run`

## Notes

- PostgreSQL data source configuration is in `src/main/resources/application.yml`
- JWT secrets and auth settings should be secured before production
